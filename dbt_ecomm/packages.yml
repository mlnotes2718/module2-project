# packages.yml
packages:
  - package: dbt-labs/dbt_utils
    version: "1.3.0" # Use the latest version, check for updates
  - package: metaplane/dbt_expectations
    version: "0.10.8" # Use the latest version, check for updates